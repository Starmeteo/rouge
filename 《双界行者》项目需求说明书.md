# 《双界行者》项目需求说明书

> 项目状态基线：2026-09-10
> 本文同时记录产品目标与代码实现边界。标为“已实现”的内容以当前 `src/main` 为准；未标记的设计不应视为现有版本功能。

## 一、项目概述

### 1.1 名称与定位

- **名称：**《双界行者》（Dual World Walker）
- **类型：** 单机、俯视角、双世界切换、Roguelike、动作射击原型
- **平台：** Windows、macOS、Linux 桌面端
- **目标：** 在单层关卡中验证“切换世界以改变战斗与通路”的核心循环。

### 1.2 技术约束

| 项目 | 当前方案 |
| --- | --- |
| 语言与运行时 | Java 21（Maven Compiler `--release 21`） |
| UI/渲染 | JavaFX 21.0.6，Canvas 2D 自绘 |
| 构建 | Maven Wrapper（`mvnw` / `mvnw.cmd`） |
| 自动化测试 | JUnit Jupiter 5.12.1 |
| 设计原则 | 模型层不引用 JavaFX；控制器处理输入与生命周期；视图只负责布局与绘制 |

## 二、核心玩法

每个房间同时对应光之界（LIGHT）和影之界（SHADOW）。玩家只能伤害与自己处于同一世界的敌人，也只会受到同世界敌方攻击的伤害。因此，清理混合世界敌人的战斗房时，切界既是输出手段，也是规避风险与绕过相位墙的路径选择。

| 世界/形态 | 视觉和移动 | 已实现攻击 |
| --- | --- | --- |
| 光之界 | 金色系；基础移动速度 | 远程光弹，有速度、寿命与墙体碰撞 |
| 影之界 | 紫色系；移动速度乘以 1.15 | 近距离扇形斩击，独立冷却与攻击判定 |

### 2.1 相位切换

- 相位能量范围为 0～100，开局满值；仅满值可切换，切换后清空。
- 当前可通过脱战自动回复、击杀敌人和拾取相位碎片恢复能量。
- 当前版本使用 **`Tab`** 切界（不是 Shift）。若目标世界位置被墙阻挡，会寻找最近的安全落点。
- 切换会触发相位脉冲，清除玩家附近敌方投射物，并有短暂的视觉反馈和切换冷却。

## 三、单局流程与当前边界

### 3.1 当前可玩流程

1. 登录或校验本地玩家档案后进入主菜单。
2. 以设置中的开发种子（可留空）开始新局，生成一张可复现的单层分支地图。
3. 在入口、战斗、奖励、商店、事件与 Boss 节点之间移动；战斗/Boss 节点生成敌人与门禁，其余节点提供资源。
4. 清空战斗房敌人后出口解锁，房间北侧出现宝箱；打开后奖励落在地面等待拾取。
5. 商店上架两件标价装备：`E` 选中并显示价格，再按一次 `E` 确认购买，走开自动取消选择。
6. 在 Boss 房与裂隙守望者战斗；其生命低于 50% 时由光界转为影界。击败首领后房间中央出现传送门，走近按 `E` 进入下一层。
7. 一局共 5 层：每层的初始房间会提示「第 N 层」，HUD 常显层数；穿过第 5 层的传送门即通关。

### 3.2 未完成流程

- 道具图鉴与道具效果、商店之外的资源消费出口尚未接入。
- Boss 的召唤与独立二阶段技能、层间差异化敌人配置尚未实现。
- 未实现局内自动存档、联网、手柄和音频。

## 四、地图与房间

### 4.1 地图规则

- `MapGenerator` 生成无环、带分支并以 Boss 节点收尾的房间图；数字和文本种子均可复现。
- 一局共 `GameConfig.TOTAL_FLOORS`（5）层，每层各生成一张地图：第 N 层的种子为本局种子加 `(N-1) × FLOOR_SEED_STEP`，因此每层地图互不相同但同一种子下可复现；玩家的生命、金币与装备跨层保留。
- 地图使用节点连接，而非固定网格。房间会记录已发现与已访问状态，小地图只显示已发现节点。
- 入口是无障碍安全房；Boss 房较大；其他房间从方形、矩形、L 形、十字形中选择，并按种子生成障碍与相位墙。
- 商店不会刷在距离入口不足 `RoomConfig.SHOP_MIN_DEPTH`（3）个房间的位置：开局既没有金币也没有选择余地。
- 出入口有方向与门禁状态。房间未清理时门为关闭状态，清理后开放。
- **房间内容只生成一次**：奖励、商店货架、宝箱与事件在第一次进入时按种子生成并保存在房间上，反复进出既不会重刷，也不会消失；已清空的怪物房再次进入不会重新刷怪。
- 小地图用细线连接相邻的已发现房间以表示连通性，当前房间可以走的门用亮线标出；房间以图标区分：`￥` 商店、`?` 事件、宝箱图标表示还有没打开的宝箱；已清空的战斗/首领房套绿框，还有未拾取物、未开宝箱或未触发事件的房间点金点。

### 4.2 房间类型与实现状态

| 类型 | 目标职责 | 当前实现 |
| --- | --- | --- |
| 入口 | 出生与安全区 | 已实现，无障碍、不刷敌人 |
| 战斗 | 清怪、开门、奖励 | 已实现清怪、开门与宝箱（开箱后奖励留在地面） |
| 奖励 | 直接获得资源 | 已实现金币与装备拾取物，内容只生成一次 |
| 商店 | 消耗金币购买道具 | 已实现两件标价装备、二次确认购买与离开范围取消选择 |
| 事件 | 祝福/诅咒选择 | 已实现一次性事件：治疗/相位碎片/装备，或伏击刷怪 |
| Boss | 层末战斗 | 已实现单 Boss 生成、跨界、基础攻击；击败后房间中央出现通往下一层的传送门 |

## 五、敌人与战斗

### 5.1 当前敌人池

| 分类 | 枚举标识 | 数量/生成规则 |
| --- | --- | --- |
| 普通敌人 | `LANTERN`、`WOLF`、`GOLEM`、`MAGE` | 战斗房随机生成 5～7 个，光/影交替分配 |
| 精英敌人 | `EXECUTIONER`、`BELL` | 后段战斗房有概率替换一只普通敌人 |
| Boss | `WATCHER` | Boss 房仅生成一名，生命值低于一半时从 LIGHT 转为 SHADOW |

敌人随层数成长（`GameConfig`）：生命值为 `基础值 × (1 + (层数-1) × 0.25)`，防御为 `(层数-1) × 1` 并封顶 3。防御只减免伤害，玩家每击至少造成 1 点，避免高层出现完全打不动的情况。

### 5.2 已实现规则

- 敌人只在与玩家同世界时更新追击与攻击；非当前世界敌人保留在模型中但不造成碰撞。
- **索敌覆盖整个房间**：同界敌人只要与玩家同处一房（半径取房间外接矩形对角线，见 `GameConfig.ENEMY_AGGRO_WHOLE_ROOM`）就会锁定玩家并主动接近，房间内不再存在“站得太远所以完全不动”的敌人；锁定后持续追击，玩家切界或离开房间才重新索敌。
- **视线判定**：被墙体/障碍挡住时敌人不会开火，而是沿房间距离场（`RoomFlowField`，粗粒度 BFS）绕行，绕行方向按自身身位半径计算；距离场不可用时退回沿障碍切线的贴墙绕行。开火视线按弹体半径探测，避免体型大的敌人因为玩家脚下放不下自己的身位就判定“看不见”。
- **脱困兜底**：持续跟踪敌人“有没有更接近玩家”，连续 2 秒既没有进步、自身也没挪出 64 像素，就把它挪到最近一个距离场给得出方向的合法位置。脱困只在走得到的范围内挪动（仅当自身已经嵌进墙体时才允许就近弹出），不会隔墙把敌人搬到玩家身边。这条规则专门对付“把首领顶在墙角”的玩法——首领身位直径 92，一旦卡在走不出去的死角就既追不上也打不到。
- **守望者的短距裂隙闪现**：当它连续 2 秒无法接近玩家、或玩家在它自己的寻路场里根本不可达（躲进了它挤不进去的角落）时，撕开一道裂隙出现在玩家附近：在玩家周围 150～420 像素的环上搜落点，优先选落地就能直接打到玩家的位置，位移上限 560 像素；落地后 0.9 秒起手、两次之间 4 秒冷却。闪现是跨空间位移，允许越过墙体——这正是“玩家躲进死角”的解法；起终点会绘制扩散的裂隙特效，避免看起来像穿墙卡顿。
- **首领房布局**：首领房的障碍物离房间边缘至少 144 像素、彼此间隔至少 112 像素（普通房分别是 96 / 28）。首领身位大，外圈通道必须宽到寻路网格采得到，否则它的距离场会把外圈判成不可达——那正是“能被顶在墙上”的根因。
- 开火距离分远程（620 像素）与近战（150 像素）两档，并各自保持站位距离（190 / 105 像素）；敌方弹体寿命由最大开火距离反推，保证最远处开火也能飞到玩家身上。
- 玩家投射物和近战斩击仅命中同世界敌人。敌方攻击也携带世界属性，仅命中同世界玩家。
- 敌人与敌方攻击会检测房间轮廓、墙体和路径；离开房间时会清理玩家的临时攻击。
- 敌人按自身身位半径选点生成（首领 46、精英 34、普通怪 27），不会出生在墙体内。
- 敌人死亡会计入清房门禁并恢复相位能量。

### 5.3 后续战斗需求

目前所有敌人使用基础追击与投射攻击框架，寻路为粗粒度距离场加局部贴墙绕行：玩家若站在只有自身身位放得下的窄缝里，敌人会聚集在缝口而无法开火。后续需要补齐前摇/释放/收招状态、差异化技能、地面攻击、预警、素材动画编排，以及 Boss 的召唤、独立二阶段技能和胜利处理。

## 六、UI 与操作

### 6.1 已实现界面

- 登录页：昵称与本地密码摘要；身份与设置分别持久化。
- 主菜单：开始游戏、道具图鉴占位页、设置、退出；登录与菜单共用双界动态背景。
- 游戏页：Canvas 房间与角色渲染、光/影 HUD、生命、攻击充能、相位能量、当前层数、两界剩余敌人、金币和探索小地图。
- 首领房清空后房间中央绘制旋转的传送门（标注目标层数），走近即可交互；打通第五层后显示「穿越完成」通关界面（`R` 重开 / `M` 返回主菜单）。
- 交互提示：跟随角色显示 `E  拾取 生命恢复药剂 / 装备 晨曦法杖 / 打开宝箱 / 触发事件`；走到物品旁边还会在物品下方显示名称标签，商店商品常显「名称 + 价格」（买不起时变灰红），选中后高亮商品与价格牌并提示“再按 E 确认”。
- 探索小地图：细线表示房间连通性（当前房间可走的门用亮线），房间内用图标区分类型（`￥` 商店、`?` 事件、宝箱），并标出已清空的战斗/首领房（绿框）与还有东西可拿的房间（金点）。
- 暂停页：Esc/P 暂停，冻结游戏循环并保留底层游戏画面；F11/Alt+Enter 切换全屏。

### 6.2 操作表

| 动作 | 输入 |
| --- | --- |
| 登录/菜单确认 | Enter 或鼠标左键 |
| 移动 | WASD 或方向键 |
| 瞄准 | 移动鼠标 |
| 攻击 | 按住鼠标左键 |
| 交互（拾取/开箱/事件） | `E` |
| 商店购买 | `E` 选中（显示价格）→ 再按 `E` 确认；走开自动取消 |
| 光/影切换 | `Tab`（相位能量满值） |
| 暂停/继续 | Esc 或 P |
| 全屏 | F11 或 Alt+Enter |

## 七、工程结构

```text
com.phantomcorridor
├── App / Launcher                启动、场景编排和窗口事件
├── config                        AppConfig、GameConfig、RoomConfig、Settings
├── controller                    登录、菜单、游戏输入与 SceneManager
├── core                          GameLoop、GameState
├── model
│   ├── GameSession               单局状态聚合
│   ├── dungeon                   MapGenerator、DungeonMap
│   ├── room                      Room、RoomLoot、RoomContentSystem、RoomLayout、Wall、RoomNavigationSystem、RoomFlowField
│   ├── entity                    Player、Enemy、EnemyKind、动画状态
│   ├── combat                    玩家攻击、敌人系统、攻击实体与投射物
│   └── effect                    WorldShiftSystem
├── util                          碰撞、随机数与资源加载
└── view                          登录、菜单、暂停、GameView、GameRenderer
```

资源位于 `src/main/resources/com/phantomcorridor/`：`sprites/` 保存玩家及怪物像素资源，`enemies/` 保存敌人图集与说明，`ui/ui.css` 保存控件样式。

## 八、里程碑与验收状态

| 阶段 | 状态 | 实际交付 |
| --- | --- | --- |
| 第 1～2 天 | 完成 | JavaFX 工程、固定步长循环、场景生命周期、登录、本地档案、主菜单与设置 |
| 第 3 天 | 完成 | 玩家移动、鼠标瞄准、光弹、影刃、角色/基础 HUD 渲染 |
| 第 4 天 | 完成 | 相位能量、`Tab` 切界、冷却、安全落点、相位脉冲与清弹 |
| 第 5 天 | 完成 | 种子地图、房间布局/墙体/门禁、房间切换、探索小地图 |
| 第 6 天 | 主要完成 | 普通/精英敌人生成、同界 AI、伤害结算、清房门禁、基础 Boss 跨界 |
| 第 7 天 | 完成 | 房间内容（奖励/宝箱/事件）只生成一次、商店标价与二次确认购买、小地图清空与拾取提示 |
| 第 8 天 | 完成 | 五层推进：首领房传送门、层数提示、敌人按层提升生命与防御、通关结算 |
| 第 9～10 天 | 未完成 | 道具图鉴与道具效果、完整 Boss 技能、平衡、音频与交付打包 |

### 验收清单（当前）

| 验收项 | 状态 |
| --- | --- |
| 能登录并从主菜单开始新局 | 已实现 |
| 随机且可复现的分支地图、战斗房和 Boss 房 | 已实现 |
| 商店不会刷在出生点附近 | 已实现（距入口至少 3 个房间） |
| 房间内容不会因反复进出而重刷或清空 | 已实现 |
| 已击败的怪物房再次进入不刷怪，并在小地图标记 | 已实现 |
| 在满相位能量下切换双世界 | 已实现（`Tab`） |
| 仅同世界敌人/攻击生效 | 已实现 |
| 远距离敌人也会主动索敌、接近并攻击玩家 | 已实现（索敌覆盖整房，含绕行寻路） |
| 敌人不会因为贴墙而永久卡死（含首领被顶在墙角） | 已实现（脱困兜底 + 首领房通道宽度 + 守望者裂隙闪现） |
| 清空双界敌人后开放出口 | 已实现 |
| 商店按标价出售、二次确认购买、离开范围取消选择 | 已实现 |
| 房间类型与状态在小地图上可辨认（连通线、`￥`/`?`/宝箱/传送门图标、已清空与有未拿取标记） | 已实现 |
| 回血道具明确提示为「生命恢复药剂」 | 已实现 |
| 一局五层、每层击败首领后出现可交互传送门并提示层数 | 已实现 |
| 敌人生命值与防御随层数提升 | 已实现 |
| 死亡/通关结算并可回到主菜单 | 已实现（覆盖层 + `R` 重开 / `M` 返回） |
| 至少三种可拾取道具及光/影流派效果 | 未实现 |
| Boss 两阶段差异机制 | 部分实现（半血跨界，无独立技能） |

## 九、开发与测试

在 JDK 21+ 环境下使用 Maven Wrapper：

```powershell
.\mvnw.cmd javafx:run
.\mvnw.cmd test
```

测试目录当前覆盖输入、玩家状态、世界切换、地图、房间导航、房间内容与商店、敌人系统与按层成长、层数推进、玩家攻击、敌方投射物、房间距离场及游戏会话等 13 个核心测试类。其中：

- 敌人索敌测试在多张真实种子地图上模拟 30 秒，校验每个同界敌人都能锁定玩家、主动接近并走到可开火的位置；
- 首领房与寻路测试校验 60 个种子下首领房内不存在“首领走不到”的落脚点（回归“把首领顶在墙角”的问题），并校验：和墙重叠的敌人会被脱困兜底挪出来、随后继续追击，脱困永远不会把敌人隔墙搬走，以及玩家躲进首领挤不进去的角落时守望者会靠裂隙闪现贴到身边、且冷却期间不会连闪；
- 房间内容测试真实走门进出商店与战斗房，校验货架不重刷、不因离开而清空、买走的商品不会刷回来，以及二次确认与离开范围自动取消选择；地图测试额外校验 300 个种子里商店都不会刷在入口附近；
- 层数推进测试走完整流程：进入首领房 → 击败首领 → 传送门出现 → 按 `E` 进入下一层，连续五层后校验通关结算，并校验金币与装备跨层保留、敌人生命值与防御随层提升。

> 最近三次改动（敌人索敌、房间内容与商店、五层推进）均在本地 JDK 25 + Maven Wrapper 环境执行过完整 `.\mvnw.cmd test`：64 个用例全部通过（三次改动前为 22 个）；`.\mvnw.cmd javafx:run` 需要图形环境，未在改动中启动。
