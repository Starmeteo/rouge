package com.phantomcorridor.model;

/**
 * 世界类型枚举（对应新需求 §10 核心数据模型约定 WorldType，§3.1 世界类型）。
 *
 * <p>玩家在任何时间处于且只处于其中一个世界；不同世界拥有各自只存在于该世界的敌人、
 * 相位墙，以及对应可攻击性的光/影形态。
 */
public enum WorldType {

    /** 光之界：默认世界；场景明亮，以金色/白色为主（§3.1） */
    LIGHT,

    /** 影之界：切换世界；场景冷色，以紫黑/暗为主（§3.1） */
    SHADOW
}
